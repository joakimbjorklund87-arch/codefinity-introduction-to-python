Front Fill Calculator — Web App

Open `index.html` in a browser to use the calculator. To serve locally (recommended) run:

```bash
cd web_app
python3 -m http.server 8000
# then open http://localhost:8000 in your browser
```

The app is a small static HTML/JS UI that replicates the original Python calculator.

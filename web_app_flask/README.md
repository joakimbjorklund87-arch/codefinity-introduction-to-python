Flask Front Fill Calculator

Run locally:

```bash
cd web_app_flask
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python app.py
# open http://127.0.0.1:5000
```

The app serves a form and shows center-to-center and edge-to-edge speaker spacing.
